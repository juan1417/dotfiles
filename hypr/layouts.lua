------------------
---- LAYOUTS ----
------------------

-- Dwindle
hl.config({
	dwindle = {
		preserve_split = true,
	},
})

-- Master
hl.config({
	master = {
		new_status = "master",
	},
})

-- Scrolling
hl.config({
	scrolling = {
		fullscreen_on_one_column = true,
	},
})
